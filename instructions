Design an aft flap configuration for a hydrofoil that minimizes drag while meeting the required lift target.

The design variables are:
- flap deflection
- hinge gap
- chord fraction
- blend radius

The submitted configuration must stay within all specified bounds and satisfy any geometric feasibility constraints. Candidate designs will be evaluated by their drag performance at the required lift condition, with penalties for infeasible geometry or non-convergent results.

Return exactly one valid design configuration in the required output format.
